apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-registry
  namespace: cicd_neotys
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
  storageClassName: gold